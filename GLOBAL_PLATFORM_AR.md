# HDB Global Hospitality Platform V2

هذه النسخة تنقل مشروع HDBIPTV من مجموعة صفحات تلفزيون إلى أساس منصة ضيافة متكاملة تشمل واجهة النزيل، مركز تشغيل الفندق، إدارة المحتوى، الأجهزة، الخدمات، والتكاملات.

## ما تم تنفيذه فعليًا

### واجهة النزيل على التلفزيون

- شاشة ترحيب شخصية مرتبطة ببيانات الغرفة والنزيل من Global Core API.
- قائمة خدمات تعمل بالريموت وتحتوي على التلفزيون المباشر، خدمات الغرفة، المطاعم، دليل الفندق، الرسائل، Secure Casting، الفاتورة والمغادرة السريعة.
- دعم عربي وإنجليزي والتنقل بأسهم الريموت وزر OK والرجوع.
- إرسال طلبات خدمات الغرفة والمطاعم والمغادرة إلى API المركزي.
- شاشة Guest Folio جاهزة للربط مع PMS.
- شاشة Secure Casting توضح دورة الاقتران الآمن؛ تشغيل الكاستنج الحقيقي يحتاج بوابة Casting معزولة على شبكة الفندق.

### مركز تشغيل الفندق

المسار:

`/control/`

ويشمل:

- مركز قيادة لحظي.
- تشغيل الفندق والغرف.
- المباني والأدوار.
- الغرف والإقامات.
- النزلاء واللغات وVIP.
- أسطول Samsung Tizen وLG webOS وأجهزة STB.
- القنوات والمصادر والنسخ الاحتياطية والجودة وUptime.
- الباقات ودليل البرامج EPG.
- Experience Studio مع معاينة 16:9 ونشر تجربة النزيل.
- طلبات النزلاء ومتابعة SLA والأقسام.
- Digital Signage.
- Integration Hub لـPMS وWISI وSamsung وLG والفوترة والكاستنج.
- التقارير.
- المستخدمون والصلاحيات.
- حالة النظام والنسخ الاحتياطي والتدقيق.

### Global Core REST API

المسار:

`/api/v2/index.php`

الموارد الأساسية:

- `ping`
- `health`
- `dashboard`
- `state`
- `properties`
- `buildings`
- `floors`
- `rooms`
- `guests`
- `devices`
- `channels`
- `packages`
- `epg`
- `requests`
- `signage`
- `integrations`
- `users`
- `experience`
- `device-command`
- `integration-test`

يدعم GET وPOST وPUT وPATCH وDELETE، التخزين الذري، سجل العمليات، معرف لكل طلب، وبداية مصادقة Token.

## التشغيل على XAMPP

1. فك الملف داخل:

   `C:\xampp\htdocs\HDBIPTV`

2. شغّل Apache.

3. افتح واجهة التلفزيون:

   `http://SERVER-IP/HDBIPTV/`

4. افتح مركز التشغيل:

   `http://SERVER-IP/HDBIPTV/control/`

5. افحص API:

   `http://SERVER-IP/HDBIPTV/api/v2/index.php?resource=ping`

6. غيّر Token الافتراضي في بيئة Apache:

   `HDB_API_TOKEN=YOUR-STRONG-SECRET`

ثم حدّثه في ملفات JavaScript أو انقل المصادقة إلى جلسة خادم قبل الاستخدام الإنتاجي.

## متطلبات الإنتاج الحقيقي

هذه النسخة أساس برمجي وتشغيلي وليست بديلًا جاهزًا لاعتمادات الشركات التجارية من دون أجهزة وبيانات الربط الفعلية. للوصول إلى تشغيل فندقي إنتاجي كامل يلزم:

- بيانات PMS أو Oracle OPERA API.
- بيانات WISI OM/Tangram أو مصدر القنوات الحقيقي.
- شهادات وتوقيع تطبيق Samsung Tizen وLG webOS.
- بوابة Casting تدعم عزل VLAN وإلغاء الاقتران عند checkout.
- قاعدة بيانات إنتاجية مثل PostgreSQL أو MySQL بدل JSON عند التوسع.
- HTTPS، إدارة أسرار، تسجيل دخول فعلي، وصلاحيات Server-side.
- اختبار على موديلات الشاشات الفعلية وشبكة الفندق.

## مبدأ البناء

لم يتم حذف صفحات التلفزيون أو ملفات القنوات الأصلية الموجودة في المشروع. تم إضافة المنصة الجديدة فوق المشروع مع الإبقاء على إمكانية تطوير مشغل القنوات الحالي وربطه بمصادر الفندق.
